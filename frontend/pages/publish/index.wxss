/* pages/publish/index.wxss */
page {
  background-color: #FDFCFB;
}

.container {
  padding: 24rpx;
  padding-bottom: 120rpx;
}

/* 类型切换标签 */
.type-tabs {
  display: flex;
  background-color: rgba(253, 252, 251, 0.95);
  border-radius: 20rpx;
  margin-bottom: 24rpx;
  padding: 8rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.05);
}

.tab-item {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  font-size: 28rpx;
  color: #8b8680;
  border-radius: 16rpx;
  transition: all 0.3s;
}

.tab-item.active {
  background-color: #2D4739;
  color: #ffffff;
  font-weight: 500;
  box-shadow: 0 4rpx 12rpx rgba(45, 71, 57, 0.35);
}

/* 表单区域 */
.form-section {
  background-color: rgba(253, 252, 251, 0.98);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.form-item {
  margin-bottom: 32rpx;
}

.form-item:last-child {
  margin-bottom: 0;
}

.label {
  font-size: 28rpx;
  color: #2D4739;
  margin-bottom: 16rpx;
  font-weight: 600;
}

.required {
  color: #D67D5C;
}

.input {
  width: 100%;
  height: 80rpx;
  padding: 0 24rpx;
  background-color: rgba(139, 134, 128, 0.08);
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #2D4739;
  box-sizing: border-box;
}

.textarea {
  width: 100%;
  height: 200rpx;
  padding: 24rpx;
  background-color: rgba(139, 134, 128, 0.08);
  border-radius: 16rpx;
  font-size: 28rpx;
  color: #2D4739;
  box-sizing: border-box;
}

.textarea.large {
  height: 300rpx;
}

.char-count {
  text-align: right;
  font-size: 24rpx;
  color: #8b8680;
  margin-top: 8rpx;
}

/* 位置选择 */
.location-picker {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 24rpx;
  height: 80rpx;
  background-color: rgba(139, 134, 128, 0.08);
  border-radius: 16rpx;
}

.location-text {
  font-size: 28rpx;
  color: #2D4739;
}

.arrow {
  font-size: 36rpx;
  color: #8b8680;
}

/* 图片上传区域 */
.image-section {
  background-color: rgba(253, 252, 251, 0.98);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.section-title {
  font-size: 28rpx;
  color: #2D4739;
  margin-bottom: 24rpx;
  font-weight: 600;
}

.image-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.image-item {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.image {
  width: 100%;
  height: 100%;
}

.delete-btn {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 40rpx;
  height: 40rpx;
  background-color: rgba(45, 71, 57, 0.85);
  color: #ffffff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  line-height: 1;
}

.image-add {
  width: 200rpx;
  height: 200rpx;
  border: 2rpx dashed rgba(139, 134, 128, 0.3);
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: rgba(139, 134, 128, 0.05);
}

.add-icon {
  font-size: 64rpx;
  color: #8DA390;
  line-height: 1;
}

.add-text {
  font-size: 24rpx;
  color: #8b8680;
  margin-top: 8rpx;
}

.image-tip {
  font-size: 24rpx;
  color: #8b8680;
  margin-top: 16rpx;
}

/* 提交按钮 */
.submit-section {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 24rpx;
  background-color: #FDFCFB;
  box-shadow: 0 -4rpx 16rpx rgba(0, 0, 0, 0.05);
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.submit-btn {
  background-color: #2D4739;
  color: #ffffff;
  border: none;
  border-radius: 48rpx;
  height: 88rpx;
  font-size: 32rpx;
  font-weight: 500;
  line-height: 88rpx;
  box-shadow: 0 4rpx 16rpx rgba(45, 71, 57, 0.35);
}

.submit-btn:active {
  opacity: 0.9;
}
