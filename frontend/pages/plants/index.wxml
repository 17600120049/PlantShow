<template>
  <view class="container">
    <!-- 顶部导航 -->
    <view class="header">
      <text class="header-title">🌿 我的植物</text>
      <view class="add-btn" bindtap="goToPublish">+</view>
    </view>

    <!-- 统计卡片 -->
    <view class="stats-card">
      <view class="stat-item">
        <text class="stat-value">{{ plantCount }}</text>
        <text class="stat-label">我的植物</text>
      </view>
      <view class="stat-divider"></view>
      <view class="stat-item">
        <text class="stat-value">{{ adoptCount }}</text>
        <text class="stat-label">领养次数</text>
      </view>
      <view class="stat-divider"></view>
      <view class="stat-item">
        <text class="stat-value">{{ breedCount }}</text>
        <text class="stat-label">繁殖代数</text>
      </view>
    </view>

    <!-- 筛选标签 -->
    <view class="filter-tabs">
      <view class="tab-item {{ activeTab === 'all' ? 'active' : '' }}" bindtap="setTab" data-tab="all">全部</view>
      <view class="tab-item {{ activeTab === 'owned' ? 'active' : '' }}" bindtap="setTab" data-tab="owned">我拥有的</view>
      <view class="tab-item {{ activeTab === 'adopted' ? 'active' : '' }}" bindtap="setTab" data-tab="adopted">已领养的</view>
    </view>

    <!-- 植物列表 -->
    <view class="plant-list">
      <view class="plant-card" wx:for="{{ plants }}" wx:key="id" bindtap="goToDetail" data-id="{{ item.id }}">
        <view class="plant-image-wrap">
          <view class="plant-emoji">{{ item.emoji }}</view>
          <view class="plant-tag">{{ item.status }}</view>
        </view>
        <view class="plant-info">
          <text class="plant-name">{{ item.name }}</text>
          <text class="plant-code">Plant ID: {{ item.plantCode }}</text>
          <view class="plant-meta">
            <text class="meta-item">🌱 {{ item.breedCount }}代</text>
            <text class="meta-item">📍 {{ item.location }}</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{ plants.length === 0 }}">
      <text class="empty-icon">🌱</text>
      <text class="empty-text">还没有植物</text>
      <view class="empty-btn" bindtap="goToPublish">去领养</view>
    </view>
  </view>
</template>

<script>
Page({
  data: {
    plantCount: 5,
    adoptCount: 3,
    breedCount: 2,
    activeTab: 'all',
    plants: [
      { id: 1, name: '绿萝', emoji: '🌱', plantCode: 'PL-001', breedCount: 3, location: '北京', status: '成长中' },
      { id: 2, name: '多肉拼盘', emoji: '🪴', plantCode: 'PL-002', breedCount: 2, location: '上海', status: '已领养' },
      { id: 3, name: '月季', emoji: '🌹', plantCode: 'PL-003', breedCount: 1, location: '广州', status: '成长中' },
      { id: 4, name: '向日葵', emoji: '🌻', plantCode: 'PL-004', breedCount: 4, location: '深圳', status: '已领养' },
      { id: 5, name: '幸运草', emoji: '🍀', plantCode: 'PL-005', breedCount: 2, location: '杭州', status: '成长中' }
    ]
  },

  onLoad: function () {
    console.log('植物页面加载');
  },

  goToPublish: function () {
    wx.navigateTo({
      url: '/pages/publish-plant/index'
    });
  },

  setTab: function (e) {
    const tab = e.currentTarget.dataset.tab;
    this.setData({ activeTab: tab });
  },

  goToDetail: function (e) {
    const id = e.currentTarget.dataset.id;
    wx.navigateTo({
      url: `/pages/plant-detail/index?id=${id}`
    });
  }
});
</script>

<style lang="scss">
.container {
  min-height: 100vh;
  background: #f5f5f5;
  padding-bottom: 120rpx;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 60rpx 30rpx 30rpx;
  background: linear-gradient(135deg, #4CAF50 0%, #8BC34A 100%);
}

.header-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #ffffff;
}

.add-btn {
  width: 60rpx;
  height: 60rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  color: #ffffff;
}

.stats-card {
  display: flex;
  justify-content: space-around;
  background: #ffffff;
  margin: -30rpx 30rpx 20rpx;
  padding: 30rpx;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.1);
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-value {
  font-size: 40rpx;
  font-weight: bold;
  color: #4CAF50;
}

.stat-label {
  font-size: 24rpx;
  color: #999999;
  margin-top: 8rpx;
}

.stat-divider {
  width: 1rpx;
  background: #f0f0f0;
}

.filter-tabs {
  display: flex;
  padding: 0 30rpx;
  margin-bottom: 20rpx;
}

.tab-item {
  flex: 1;
  text-align: center;
  padding: 15rpx;
  font-size: 28rpx;
  color: #999999;
  border-radius: 30rpx;
  transition: all 0.3s;
}

.tab-item.active {
  background: linear-gradient(135deg, #4CAF50 0%, #8BC34A 100%);
  color: #ffffff;
}

.plant-list {
  padding: 0 30rpx;
}

.plant-card {
  display: flex;
  background: #ffffff;
  border-radius: 20rpx;
  padding: 20rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.05);
}

.plant-image-wrap {
  position: relative;
  width: 120rpx;
  height: 120rpx;
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
}

.plant-emoji {
  font-size: 60rpx;
}

.plant-tag {
  position: absolute;
  top: -10rpx;
  right: -10rpx;
  background: #FF9800;
  color: #ffffff;
  font-size: 20rpx;
  padding: 5rpx 15rpx;
  border-radius: 20rpx;
}

.plant-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.plant-name {
  font-size: 30rpx;
  font-weight: bold;
  color: #333333;
  margin-bottom: 10rpx;
}

.plant-code {
  font-size: 22rpx;
  color: #999999;
  margin-bottom: 10rpx;
}

.plant-meta {
  display: flex;
}

.meta-item {
  font-size: 22rpx;
  color: #666666;
  margin-right: 20rpx;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 100rpx 0;
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: 20rpx;
}

.empty-text {
  font-size: 28rpx;
  color: #999999;
  margin-bottom: 30rpx;
}

.empty-btn {
  background: linear-gradient(135deg, #4CAF50 0%, #8BC34A 100%);
  color: #ffffff;
  padding: 20rpx 60rpx;
  border-radius: 40rpx;
  font-size: 28rpx;
}
</style>